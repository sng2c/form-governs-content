# Form templates

Each file here is a single Python-format template string (with `{facts}` and `{question}`
placeholders) plus a leading philosophical note as a comment. The `runner/forms.py` loads
these as a dict.